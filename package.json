{
  "scripts": {
    "buildall": "lerna run build --since"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "chaingun-workspace",
  "devDependencies": {
    "husky": "^4.2.1",
    "lerna": "^3.20.2"
  }
}
